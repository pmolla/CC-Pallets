<odoo>
    <record id="view_account_inventory_form" model="ir.ui.view">
        <field name="name">account.inventory.form</field>
        <field name="model">account.inventory</field>
        <field name="arch" type="xml">
            <form string="Cuenta Corriente de Inventario">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="partner_id"/>
                        <field name="product_id"/>
                    </group>
                    <notebook>
                        <page string="Movimientos de Inventario">
                            <field name="inventory_move_ids" readonly="1">
                                <tree>
                                    <field name="product_id"/>
                                    <field name="quantity_done"/>
                                    <field name="date"/>
                                    <field name="location_id"/>
                                    <field name="location_dest_id"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_account_inventory_tree" model="ir.ui.view">
        <field name="name">account.inventory.tree</field>
        <field name="model">account.inventory</field>
        <field name="arch" type="xml">
            <tree string="Cuenta Corriente de Inventario">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="product_id"/>
            </tree>
        </field>
    </record>
</odoo>
